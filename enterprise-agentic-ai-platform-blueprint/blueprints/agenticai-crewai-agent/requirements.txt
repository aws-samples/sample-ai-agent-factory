# Pinned to exact tested versions (security review: supply-chain hygiene).
# Bump deliberately after re-running test_real_integration.py against the new set.
# crewai 0.41.x carried CVE-2026-2286 (SSRF in RAG search tools) and
# CVE-2026-2285 (path traversal in the JSON loader); this pin is well past
# those affected releases. Verify the CrewAI security advisories before any
# further bump.
crewai==0.86.0
boto3==1.35.90
pytest==8.3.4
